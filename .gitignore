# Secrets — never commit
.env
CREDENTIALS.md

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# Transient / regenerable run artifacts (see PLAN.md §14 — results/ is committed, runs/ is not)
runs/
cache/

# OS
.DS_Store
Thumbs.db

# Local Claude Code tooling/session state (machine/session-specific, not part
# of the submitted application) -- agent definitions, hooks, settings, skills,
# and session lock files all kept local, not part of the public submission.
.claude/

# Internal development/process artifacts -- kept locally, not part of the
# reviewer-facing public submission.
FINDINGS.md
PROGRESS.md
PLAN.md
KICKOFF_PROMPT.md
ERRORS.md
CLAUDE.md
