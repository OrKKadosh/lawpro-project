# Progress

Update this after each meaningful chunk of work — not just at session end.
Keep entries short. This file's job is to let a fresh session (or you,
tomorrow) pick up instantly without re-reading everything.

## Status: implementing (PLAN.md §16 implementation order)

## Done
- Step 1: `.gitignore`, `pyproject.toml`, `evalkit/` package scaffolding, `prompts/`/`results/`/`runs/`/`tests/` dirs. No API calls.
- Step 2: `evalkit/budget.py` + `evalkit/llm.py` (JSON-mode, one repair retry, prompt/response logging). Smoke-tested against the free `GET /v1/budget` only.
- Step 3: `evalkit/discover.py` + `evalkit/reference/audit.py` (risk-based source audit). Ran for real against both cases — `results/timeline_eval/case-vance_source_audit.json`, `case-davis_source_audit.json`. Finished reading the two previously-unaudited Davis OCR docs. Corrected an earlier manual finding: golden's main ORIF-surgery event is dated `09-04` (billing date), not `09-07` — golden fell into the billing-vs-operative-date trap itself. See `FINDINGS.md`.
- Step 4: `evalkit/extraction/candidates.py` (recall-oriented candidate extraction call) + `evalkit/extraction/chunk_experiment.py` (bounded chunking experiment: 3 Vance page windows × 3 chunk configs, frozen local dev set built first). Hit and fixed a real issue: dense billing-doc chunks caused truncated JSON (model was extracting individual billing line items as events) — tightened prompt scope, raised `max_tokens`, added truncation-salvage to `llm.py`. Re-run completed cleanly. **Frozen config: large (~10pp + 1pg overlap)** — 100% recall/date-fidelity across all configs, large wins on duplicate-candidate rate and cost. See `DECISIONS.md` and `results/chunking_experiment.md`.

- Step 5: built the remaining pipeline modules (`normalize.py`, `cluster.py`, `conflict.py`, `hitl.py`, `canonicalize.py`, `salience.py`, `project.py`, `prepare.py` orchestrator). Smoke-tested with synthetic data before spending real money — caught and fixed a real bug pre-run (near-date clustering pass was merging genuinely distinct routine visits). Ran the full pipeline for real on both cases: **Vance 306 candidates → 145 final timeline events, Davis 169 candidates → 114 final timeline events, both 0 schema-validation problems.** Confirmed by direct inspection: the billing-vs-operative-date trap resolves correctly (09-07, not 09-04), and the alcohol/intoxication finding golden misses IS captured. Found and fixed two more real bugs by inspecting actual output (both now have regression tests): a laterality-duplication template bug, and a more serious one — extraction had injected real-world CPT-code knowledge not stated in the source into `clinical_facts`, which the "templating = grounding" design hadn't actually guarded against; added a real word-overlap grounding check. Also replicated (didn't fix) one known limitation: the Foster/Turner consult misattribution golden has, our extraction has too. All in `FINDINGS.md`/`DECISIONS.md`. 5 pytest tests, all passing (`tests/test_extraction_pipeline.py`).

- Step 6: refactored `audit.py` to be reusable (`audit_events`, generalized from `audit_reference_timeline`) and built `evalkit/reference/compare.py` (deterministic date+type+keyword alignment, no LLM call for the alignment itself; reuses the audit methodology from step 3 to score our own candidate timeline's source-grounded precision, applying it consistently rather than inventing a separate metric). Ran for real: **Davis reference agreement 79.5%, source-grounded precision 98.7%. Vance reference agreement 50.0%, source-grounded precision 97.4%.** Vance's lower agreement number diagnosed, not just reported: 70% of unmatched golden events are routine PT visits our salience-compression stage deliberately compresses, plus golden's own manufactured "discrepancy" event (correctly absent from ours). Spot-checked the "additional events" lists (56 Vance / 58 Davis) directly — real, source-grounded facts golden/input_timeline miss (EMS mechanism of injury, elevated serum ethanol, several ED diagnoses), not noise. Results in `results/timeline_eval/case-vance.json`, `case-davis.json`.

- Step 7: built `evalkit/judge/material_facts.py`. Built strictly from each case's BENCHMARK timeline (golden/input_timeline, not our own richer extraction) — deliberately, so a summarizer is never graded on facts it was never given. Ran for both cases ($0.08, 2 calls), spot-checked the output by hand (categories, weights, compression of repetitive PT courses, no fabricated facts), fixed one cosmetic category-label inconsistency, froze both to `results/material_facts/`. Vance: 20 facts / 94 events. Davis: 17 facts / 39 events.

- Step 8: `evalkit/benchmark/controlled.py` — loads both cases' 8 pre-generated summaries, groups into 4 same-tool run1/run2 pairs each. No API calls (pure file loading).

- Step 9: `evalkit/judge/faithfulness_coverage.py` + `evalkit/judge/stability.py` + `evalkit/scoring.py` + `evalkit/report.py`. Ran for real, both cases: 16 faithfulness+coverage+usefulness judge calls + 8 pairwise-stability calls, 24 calls total, $1.319. Full results in `results/controlled_benchmark_case-vance.json`, `case-davis.json`, `results/final_scorecard.md`. Headline: **case-vance — only D is ship-eligible** (A/B/C all trip the critical-error gate); **case-davis — A, C, D ship-eligible, B is not**. The gate correctly caught the known-planted Vance B-run2 fabrications (wrong disc level, fabricated provider name) and the Foster/Turner overclaim independently in both A and B — confirms the gate mechanism works as designed, not just in theory. Investigated one nuance before accepting D's Vance result at face value: D's two runs show 1 critical-tier cross-run disagreement (run1 says Foster performed the surgery, run2 hedges to "arranged consultation") — checked against golden, run1 is the more accurate reading (golden attributes the surgical-procedures-performed line directly to Foster, no hedge), so this is a stability weakness in D's run2 (under-confident, not fabricating), not a mark against run1's individual faithfulness. Full findings in `FINDINGS.md`.

## In progress
- Responding to user feedback: before trusting an end-to-end demo, going back to improve extraction quality itself (chunk size was tuned against a frozen dev set already, but the extraction prompt and conflict-resolution/clustering behavior had only been bug-fixed reactively, never systematically audited/iterated).
- Audited real conflict-resolution output from both cases and fixed a real clustering bug found this way: `cluster.py` was merging genuinely distinct same-day events (e.g. 4 different medications) because their notes share templated boilerplate text, ignoring a clean, specific `clinical_facts` name field that disagreed. Fixed, regression-tested, re-ran free (no new API cost, deterministic stages only) on both cases' cached candidates: case-vance conflicts 128→58 / final events 145→160; case-davis conflicts 63→13 / final events 114→146; 0 schema problems either case. Full detail in `DECISIONS.md`/`FINDINGS.md`.
- Extraction-prompt iteration experiment completed (`results/prompt_experiment.md`, 12 calls, $1.448): recall/date-fidelity/duplicate-rate unchanged between variants; grounding trap confirmed fixed (baseline VIOLATED → reinforced clean on the real CPT-27535 line). Attribution trap's first result was a false positive (test window too narrow to reproduce the real bug) — caught before trusting it, re-verified against the actual full 9-page chunk that produced the real failure (2 more calls, $0.2651): baseline wrong there too, reinforced correctly named Turner. **Reinforced prompt frozen**, now the default in `candidates.py`. Full detail in `DECISIONS.md`.
- First full-extraction re-run attempt (task `bfpfz75th`) crashed on a transient `504 Endpoint request timed out` from the platform on its very first call — no cost logged, cached files untouched, but the batch has no per-document checkpointing so the whole run had to restart from zero. Added a short transient-retry (one retry after 3s, only for 5xx platform errors, distinct from the existing JSON-repair retry) to `evalkit/llm.py` so one flaky call doesn't kill an entire multi-dollar run again.
- Re-ran full extraction on both cases with the frozen prompt + transient-retry protection (task `bf7r5jnij`): succeeded cleanly, 22 calls, $2.4066, 0 schema problems either case. Confirmed both prompt fixes in the real fresh output (Turner attribution, CPT-27535 grounding both correct).
- Found and fixed a **second** real bug in the fresh output, in `salience.py`'s compression stage (not clustering) — same class of problem, one stage downstream: routine events were grouped for compression purely by `(type, provider)`, so 36 Davis medication events sharing `provider: None` all fell into one bucket and got compressed to first+last regardless of which drug (Cefazolin vanished from the final timeline entirely). Fixed in two corrected passes — the first fix looked right but real-data testing caught it reintroducing contamination through transitive union-find bridging (a compound "Oxycodone and Gabapentin" mention chained 26 different medications into one group); the second, final fix (exact-key grouping for `medication` only, fuzzy pooling for the rest) was verified correct against real data: medication groups now top out at 10 (per-drug), and PT-visit compression correctly returned to full per-provider courses. Full story, including both false starts, in `DECISIONS.md`/`FINDINGS.md`. 17/17 tests passing (2 new regression tests).
- **Final extraction numbers (frozen prompt + clustering fix + salience fix, both cases)**: case-vance final timeline events 145→202; case-davis 114→140. Both 0 schema-validation problems. `runs/candidate_timelines/*.json` now reflect this final state.
- **Stage-0 refresh done**: added a proper `__main__` runner to `evalkit/reference/compare.py` (it had only ever been run ad-hoc) and re-ran against the final extraction output. Source-grounded precision — the most direct read on our own extraction's accuracy — improved for both cases: Davis 98.7%→100%, Vance 97.4%→98.0%, confirming the grounding fix's real-world effect. Reference/date agreement shifted a few points in both directions (expected noise from a differently-composed candidate set, not a regression). 12 calls, $0.4154. Full numbers in `FINDINGS.md`.

## Golden-as-ground-truth pivot (user-directed increment, plan approved)

Responding to four points of user feedback after reading the data-reading-guide artifact: risk of overfitting, a request to treat golden as ground truth for extraction scoring (issues found in it corrected rather than left aside), a request to slow down and understand before spending, and a request to make the evaluation data genuinely readable without raw-JSON literacy. Plan approved and executed:

- **Corrected golden created**: `golden/case-vance.timeline.corrected.json` — exactly 5 changes, each sourced only from the already-completed independent source audit (never from our own candidate extraction), verified as an exact diff against the original before trusting it. Original `golden/case-vance.timeline.json` untouched. Full citation list in `DECISIONS.md`. The controlled benchmark (`report.py`) keeps using the *original* golden — a new `Case.corrected_reference_timeline_path()` accessor is used only by extraction scoring, `Case.reference_timeline_path()` (used by the controlled benchmark) is completely untouched.
- **Severity-tiered extraction scoring built**: `evalkit/reference/compare.py`'s `score_extraction_against_golden()` — four tiers (missing/major, matched-contradicted/critical, extra-grounded/minor, extra-ungrounded/critical), reusing `evalkit/scoring.py`'s exact severity weights rather than a new scale. Pure scoring math (`_score_items`) unit-tested with 9 new hand-built cases *before* any real spend; full wiring (including `corrected_reference_timeline_path` selection) dry-run end-to-end with mocked API calls, $0 cost, before running for real — the "understand before spending" commitment applied concretely this time, not just stated. Real run: case-davis done ($0.12, 5 calls, including 1 ambiguous-match check), case-vance in progress.
- **Human-readable conflict review built**: `evalkit/report_readable.py` → `results/conflict_review_<case>.md`, fully deterministic (re-runs already-free pipeline stages on cached candidates, zero API cost). Directly answers the cl003 comprehension question from raw output alone: confirmed real output now reads "3 mentions of what looks like the same real event, documented independently in: 01_ems_and_ed p1, 01_ems_and_ed p3, 02_hospitalization p1... They disagree on who documented it..." — case-vance: 41 of 315 clusters flagged as worth a human's attention, everything else resolved cleanly and isn't listed.
- Extraction pipeline logic (clustering, conflict resolution, salience, projection) kept exactly as-is per the user's explicit choice — this increment is scoring/presentation only.
- **Caught and fixed a real flaw in the new scoring before reporting any result from it**: the first real run flagged 6 "critical contradictions" for Vance. Reading every one directly (not just trusting the composite number) showed all 6 were bad greedy-alignment pairings between unrelated events, not real disagreements — the weak-match-score heuristic driving which pairs got an LLM check turned out unreliable in both directions (missed nothing real, but also wasn't a safe proxy — some legitimate matches scored as low as 0.13). Redesigned: every matched pair now gets checked, with a third judge option (`different_events`) that correctly reclassifies a bad pairing as a missed fact + a separate extra, instead of a false contradiction. New regression test encodes the exact scenario that broke the first version. Full account in `DECISIONS.md`.
- **Final verified real results**: case-vance composite 0.582, 1 confirmed fabrication (a real, previously-unnoticed misattribution — a 2022 X-ray incorrectly credited to the radiologist who only mentioned it in passing in a later report), 0 false contradictions. case-davis composite 0.694, 1 real matched-contradicted item (a follow-up visit where our extraction omits a documented complication), 0 fabrications. Full detail in `FINDINGS.md`.
- 27/27 tests passing (10 new).

## User pushed back on the first real result ("the results look bad") — three more real scoring bugs found and fixed

Right response, not a misunderstanding: investigated directly rather than either dismissing the concern or defending the number as-is. Found and fixed, each verified against real data before re-running:
1. `align_events` required exact date + exact type to match. Golden's two most material facts (the Foster/Turner pre-existing-condition findings) are undated in golden, so they could never match anything regardless of content — a structural false-negative, not a real gap. Relaxed: an undated or type-mismatched pair can still match on strong content overlap (`UNDATED_MIN_SCORE=0.2`, `CROSS_TYPE_MIN_SCORE=0.3`, the lower of the two when both apply — a first version wrongly stacked them as an AND, caught and fixed with a regression test before it shipped).
2. `AuditReport` treated any "partial" support verdict as grounded even when a specific issue was also flagged — silently absorbing a manually-confirmed fabrication (the Harrison/X-ray misattribution) into "grounded, minor." Fixed at the source (`audit.py`'s `issue_by_index`), which also corrected the already-reported `source_grounded_precision` number, not just the new scoring.
Three full re-runs, each verified against real output (and, for the free/deterministic parts, verified before spending) rather than trusted blind. 33/33 tests passing (6 more new).

**Final, fully-corrected results**: case-vance composite 0.529 (8 confirmed fabrication-adjacent issues, 4 of which cross-validate against findings from the original step-3 audit months ago), case-davis composite 0.605 (2 confirmed issues). source_grounded_precision, corrected: 0.911 / 0.943. Full breakdown and honest reading of what the composite actually measures in `FINDINGS.md`.

## Step 10: end-to-end pipeline demo — done

`evalkit/benchmark/pipeline_demo.py` — our own final extracted timeline through a real `/v1/summarize` call (case-vance→D, case-davis→A, per `results/final_scorecard.md`'s already-computed winners), judged the same way. Dry-run with mocked calls first ($0 cost) to verify wiring before spending real money. Real run: 4 calls, $0.2609. Both fail the faithfulness gate on this uncontrolled run — verified both gate-triggering claims directly against the actual timeline events before reporting (both confirmed real, not scoring artifacts): a hedge our own extraction correctly preserved got flattened to certainty by the summarizer (case-vance), and a real numeric contradiction plus a physician misattribution (case-davis). Documented in `FINDINGS.md` with the correct framing — informative about the extraction→summarize handoff, not a reason to doubt the controlled-benchmark recommendation, which stays fully separate by design.

## Step 11: skipped (optional, low priority, flagged not silently dropped)

## Step 12: WRITEUP.md — done

The core deliverable. Per-case recommendation (case-vance → D, high confidence; case-davis → A, moderate-high confidence, D as a close alternative), the "no single global answer" conclusion stated as an evidence-based finding rather than a hedge, confidence levels justified concretely, 5 specific things that would change the recommendation, the end-to-end demo's findings folded in with correct framing (informative, not shipping-relevant), scope decisions, and a reproducibility section pointing at real `results/` files. No new API cost — pure writing from already-verified evidence.

## Step 13: CLI — done

`evalkit/cli.py`, stdlib `argparse` (no new dependency), thin wrapper over already-tested modules: `list-cases`, `extract`, `evaluate-timeline`, `summarize`, `evaluate`, `run`. `list-cases` and `--help` verified for real; every other subcommand's argument-dispatch wiring verified with mocked functions (zero API cost) before being trusted.

**Real incident during this step, caught and fully recovered — logged in `ERRORS.md`**: a smoke test's mock didn't stop the real file-write step, and `cmd_extract`'s test overwrote the real `runs/candidate_timelines/case-vance.json` (202 real events) with a fake 0-event stub. Caught immediately by checking the file's actual content rather than trusting the test's "OK" output. Recovered at zero new API cost by reconstructing the 337 raw candidates from `runs/prompt_log.jsonl` (which logs every real LLM response) and re-running the already-fixed deterministic pipeline stages on them — result matched the lost file exactly (337→315→42→202, spot-checked identical content). A backup of the recovered raw candidates is kept at `runs/case-vance_candidates_recovered.json`.

## Step 15: adversarial review pass — done, 2 real issues found and fixed

Ran the `eval-reviewer` subagent fresh-context over the whole project (not just a self-check). Findings:
- **Real, fixed**: `evalkit/cli.py`'s `summarize` subcommand never tracked/logged spend (violates CLAUDE.md's non-negotiable "log and surface running spend" for every `/v1/summarize` call) — added a `BudgetTracker`.
- **Real, fixed**: `evalkit/benchmark/pipeline_demo.py` hardcoded `{"case-vance": "D", "case-davis": "A"}` — a literal violation of CLAUDE.md's "no hardcoding to specific cases" rule, even though it was in a narrow, disclosed demo module. Replaced with `choose_tool_for_case()`, which reads the ship-eligible tool with highest `mean_faithfulness` dynamically from that case's own `results/controlled_benchmark_<case>.json` — verified it produces the identical real answers (D, A) after the change, so no result changed, only how it's derived.
- **Minor, fixed**: `cli.py`'s `evaluate` subcommand always labeled output `run=1` regardless of the actual summary file — added `--run`.
- **Everything else checked and found solid**: no prose/correctness conflation in scoring, no golden-for-both-cases assumption, A/B/C/D case-locality respected throughout, stability numbers genuinely used (not decorative), confidence levels reasoned from specific evidence not asserted, `.gitignore` correctly protects secrets, the faithfulness gate (the most safety-critical logic in the project) has direct, well-targeted unit tests. Several specific numeric claims in `FINDINGS.md`/`WRITEUP.md` spot-checked directly against the underlying JSON and matched exactly.

All 33 tests still passing after the fixes; the dynamic tool-selection change verified against real data (still resolves to D/A) before trusting it.

## Application-compliance audit, prompted by direct user questions — real gaps found and fixed

User asked three pointed questions after the CLI was built: (1) are the prints clear, (2) should the interface be friendlier, (3) does this actually satisfy the brief's application requirements, specifically dynamic case discovery and "run from a clean checkout." Checking #3 honestly (not just asserting compliance) surfaced real gaps the earlier adversarial review had missed, since it reviewed methodology/code correctness, not "does a fresh clone actually run":

- **`data/` (17MB) and `golden/` were completely untracked by git** — a real clean checkout would have had no case data at all.
- **No README, no `.env.example`** — no setup path for a reviewer at all.
- **The git repo root was the whole home directory**, with an unrelated pre-existing staged changeset in it (flagged at the very start of this project, never resolved until now).
- **Discovery itself was verified, not just asserted**: created a genuine third synthetic case under `data/` and confirmed the CLI found it with zero code changes — proving `discover_cases()` really is generic, not hardcoded.
- **A deeper gap found while verifying discovery further**: even with a case discovered, `evaluate`/`run` would have crashed on any case without a pre-existing golden/`input_timeline.json` (i.e. any genuinely new case), because the material-fact-set builder required one. Fixed: `get_or_build_material_facts()` now falls back to building the fact checklist from our own extracted timeline when no external reference exists — the realistic "future analysis" scenario the brief asks for. `cmd_run`'s inline call to the timeline-accuracy check was given the same graceful handling at the time.

**Correction, caught by the mandatory `eval-reviewer` pass below**: the line above originally claimed "`evaluate-timeline` also now degrades gracefully... for a case with no reference at all" — that was only true for `cmd_run`'s *inline* call, not the standalone `evaluate-timeline` subcommand, which still crashed uncaught (`evalkit/cli.py`'s `cmd_evaluate_timeline` had no `try/except` around `stage0_evaluate`). A reviewer running exactly the individual-stage command `README.md` lists would have hit an unhandled traceback. Fixed for real this time, with a regression test (`tests/test_review_fixes.py`) — noted here plainly rather than quietly editing the claim above to look like it was always accurate.

**Fixed, all real and verified, not just asserted:**
- `evalkit/cli.py`: added an interactive case picker (`prompt_choose_case`) — running `python -m evalkit.cli` with no arguments lists every detected case and lets you pick one, then walks the full pipeline. Explicit subcommands still work for finer control.
- `evalkit/budget.py`: replaced raw internal category names (`stage0_candidate_audit`, etc.) in user-facing prints with plain-English labels; `runs/budget_log.jsonl` keeps the precise technical category for anyone who wants it.
- `evalkit/report_readable.py`: added `render_judge_output()` — the same plain-English treatment the extraction/conflict side already had, now applied to summary evaluation too (which specific claims failed, quoting exactly why, what material facts were missed).
- `evalkit/judge/material_facts.py`: `get_or_build_material_facts()`, the new-case fallback described above.
- Initialized a clean git repo scoped to just this directory (nested repo, home-directory repo untouched), added `README.md`, `.env.example`, committed everything needed to run (data/golden/code/tests/results), explicitly excluding `.env`/`CREDENTIALS.md`/`runs/`/local session-lock files. **Verified for real**: cloned to a fresh temp directory and ran `list-cases` + the full `pytest` suite there — both passed with zero pre-existing state.
- One process note: briefly ran `git config user.name` locally out of habit before checking whether it was needed — it wasn't (global config already had it) — caught immediately and reverted. Worth naming plainly rather than glossing over, even though it was low-stakes.

10 new tests added (`tests/test_cli.py`, `tests/test_material_facts_fallback.py`) covering the interactive picker and the new-case fallback logic — 43/43 passing.

## Final live verification: the actual updated CLI, run for real

`python -m evalkit.cli run case-davis` — the exact command a reviewer would run, with all of today's fixes (readable stage-by-stage output, plain-English claim breakdown, friendly budget labels) — executed for real, not mocked. 6 calls, $0.2381. Confirmed working correctly end to end: reused the cached timeline, ran the accuracy check, picked tool A, generated a real summary, and printed a genuinely readable claim-by-claim breakdown. Found and verified one more real faithfulness issue (tool A stated "Dr. Cho performed" an amputation the timeline correctly attributes to Dr. Vance as the actual surgeon, with Cho only having planned it) — confirmed directly against the candidate timeline before trusting it, and reconciled the numbers with the earlier pipeline-demo run in `FINDINGS.md` (this overwrote `results/pipeline_demo_case-davis.json` with a fresh, independently-real result; the composite differs from the first run because `/v1/summarize` isn't deterministic call-to-call, not because of a regression).

Two commits made this session (both reviewed before committing, nothing destructive): the initial scoped repo (125 files: all code, data, golden, results, docs, tests — `.env`/`CREDENTIALS.md`/`runs/` correctly excluded), and this documentation follow-up.

## Mandatory review pass (CLAUDE.md's new workflow rule): code-reviewer + eval-reviewer, both fresh-context, run in parallel

**eval-reviewer found one real, high-priority problem**: `cmd_evaluate_timeline` (the standalone `evaluate-timeline` subcommand) crashed uncaught on any case with no reference timeline — directly contradicting a claim this file had just made (see correction above). Fixed with the same `try/except FileNotFoundError` pattern `cmd_run` already used, with a regression test.

**code-reviewer found two real, high-priority problems, independent of eval-reviewer's finding**:
1. Neither `/v1/summarize` call site (`cli.py`'s `cmd_summarize`, `pipeline_demo.py`'s `run_pipeline_demo_for_case`) ever called `tracker.check_floor()` before the paid call — the budget safety floor was enforced for every `/v1/generate` call (via `call_json`) but for zero `/v1/summarize` calls anywhere in the codebase, leaving the single most expensive call type in the whole pipeline unprotected. Fixed both call sites.
2. `evalkit/reference/audit.py`'s per-document judge-call loop caught `Exception` broadly to tolerate a bad OCR file or malformed judge response without killing the whole audit — but that same broad catch silently swallowed `BudgetExceeded` too, letting the loop keep iterating (re-raising and re-catching the same exception on every remaining document, spending nothing but producing a silently-incomplete `source_grounded_precision` with no signal it was cut short by budget rather than data quality). Fixed: `BudgetExceeded` is now re-raised specifically; an ordinary per-document failure is still tolerated as before (both directions covered by a new test).

Both reviewers independently flagged the same medium-priority item: `cmd_run`'s output-path branch hardcoded `case.case_id in ("case-vance", "case-davis")`. Fixed by generalizing to "does this case have an established controlled-benchmark result" (checking for `results/controlled_benchmark_<case>.json`'s existence) — same behavior for the two known cases, correct behavior for any other case, no hardcoded names.

code-reviewer also flagged that none of the system prompts reading raw OCR/timeline text had an explicit "treat this as data, not instructions" line, relying only on structural placement (data goes in the prompt, never the system prompt) as the injection defense. Added a one-line explicit instruction to all four prompts that process document-derived text (`candidates.py`, `audit.py`, `material_facts.py`, `compare.py`'s agreement check) as defense-in-depth.

Also addressed: `get_or_build_material_facts()`'s new-case fallback now stamps `human_spot_checked: False` into the fact set it saves, rather than only disclosing the skipped verification step in a docstring — visible in the data itself, not just to someone who reads the source.

Both reviewers independently confirmed the core recommendation, the controlled-benchmark isolation, case-locality of A/B/C/D, and every spot-checked number in `WRITEUP.md`/`FINDINGS.md` against the underlying `results/` files — nothing in the later application-focused work touched or contaminated the evidence behind the recommendation.

5 new regression tests (`tests/test_review_fixes.py`), one per real bug found — 48/48 passing.

## Project status: all 15 implementation-order steps complete, application-compliance gaps found and fixed, mandatory adversarial review pass complete (5 real bugs found across two independent reviewers, all fixed and regression-tested), committed to a clean scoped repo, and verified working end-to-end with a real live run of the exact command a reviewer would use.
- Several new, real findings from this increment are not yet fixed in the extraction pipeline itself (out of scope for this increment, which kept the pipeline unchanged): the Harrison/X-ray misattribution and 7 other flagged issues (Vance), 2 flagged issues (Davis), the Davis phantom-limb-pain omission. Candidates for a future pass, alongside the already-known Foster/Turner-flavored attribution limitation.

## Final pre-submission correction pass: 7 real bugs confirmed and fixed at the root cause, 1 concern verified as already-correct, recommendation re-derived from scratch (not preserved by default)

A structured adversarial pre-submission review (16 numbered items, spanning the judge/scoring layer, extraction pipeline, CLI, and repo hygiene) was independently re-verified against the actual code and committed artifacts, item by item, before anything changed. Full account, including every reverted wrong turn along the way, in `FINDINGS.md`'s "FINAL PRE-SUBMISSION CORRECTION PASS" section — this entry is a pointer, not a duplicate.

**Real bugs confirmed and fixed**: (1) the faithfulness judge conflated summary omissions with unsupported factual claims, wrongly lowering faithfulness for content the summary never asserted — fixed at the scoring layer with a deterministic filter, not just a prompt tweak; (2) the pairwise-stability metric silently excluded run-only content from its denominator, letting a run that dropped most of its material facts still score a perfect 1.0 — replaced with two explicitly-separate, always-reported metrics; (3) `get_or_build_material_facts()` graded a summary generated from our own candidate timeline against material facts derived from the BENCHMARK timeline (a different event set entirely) — redesigned to bind coverage strictly to the exact timeline hash of what was actually summarized; (4) case-davis's source-page provenance was silently malformed (`"p1"` string instead of int → `"document pp1"` in 75/140 final events), which the audit parser then silently dropped with zero record — completion was actually 43.75% (35/80) behind a flat-looking 94.3% precision number; fixed at the normalization root, audit completion is now an explicit, un-hideable field, and case-davis's real completion is now 100% (86/86), precision 88.4% among completed; (5) a synthetic CLI test fixture lived inside production `data/`, indistinguishable from a real case — moved to `tests/fixtures/`; (6) a new case with no prior benchmark result hard-exited demanding `--tool` even in an interactive session — now prompts directly when stdin is a real terminal; (7) a truncated extraction response was silently indistinguishable from a complete one — `call_json` now treats truncation as a hard failure by default, and extraction's opt-in salvage path auto-splits and retries a truncated chunk, recording anything still incomplete explicitly. One additional item (the "corrected golden" naming) was verified as methodologically ALREADY CORRECT — the boundary the reviewer asked about genuinely holds — and was renamed to `.adjudicated.json` purely for clarity, not because the behavior was wrong.

**Recommendation re-derived from scratch, not preserved by default**: case-vance's winner is unchanged (D, now with a clean 1.00 faithfulness composite). case-davis's winner is also unchanged (A) but the REASONING materially changed: faithfulness among the three gate-passing tools is now essentially tied (previously A had a clear lead), so the recommendation now genuinely rests on coverage, and D's previously-cited "perfect stability" advantage doesn't survive the stability-metric fix (A's content-overlap stability, 0.85, is actually higher than D's, 0.77). `WRITEUP.md` was rewritten to reflect this honestly.

**Total live budget spent: $1.008** (of $4.1608 available at the start of this pass) — spent only on artifacts a fix had actually invalidated; the controlled benchmark's raw judge evidence needed no new API calls at all (pure re-scoring). 33 new regression tests added across 9 test files. Full test suite: 94/94 passing.

## Budget spent so far (update)
- **$14.836 / $20.00 cap** — three additional golden-pivot scoring re-runs this session (first flawed version $0.472/$0.497, corrected-alignment version $0.497, final issue-tiering version $0.491) on top of the earlier $12.886 total. ~$5.16 remaining.

## Next
- Step 10: end-to-end pipeline demo (our own extracted candidate timeline fed into the tool(s) the controlled benchmark identifies as strong), separate results path.
- Step 11: optional sparse-input adversarial probe, only if budget/time remain.
- Step 12: `results/` final artifacts, `final_report.md`, `WRITEUP.md` (the actual recommendation deliverable).
- Step 13: CLI (thin layer over already-working modules).
- Step 15: adversarial `eval-reviewer` subagent pass before declaring done.

## Open questions / blockers
- **Budget estimate recalibration confirmed**: extraction (step 5) cost $2.29 for 22 calls (~$0.10/call), consistent with the chunking experiment's rate — `PLAN.md` §12's original ~4-5x-lower estimates are now confirmed as a pattern, not a one-off. Still comfortably affordable (see below). Will do one pass reconciling §12 with real numbers once the remaining phases (end-to-end demo) confirm the full picture.
- Git repo root is `C:/Users/wix` (home dir) with a large pre-existing unrelated staged changeset (Elasticsearch install from Downloads) — flagged to the user, not touched, no git operations performed yet.
- The Foster/Turner attribution-defaults-to-signing-physician limitation in our own extraction pipeline (`FINDINGS.md`) is still flagged, not fixed — candidate for the write-up's "known limitations" section rather than a last-minute fix.

## Budget spent so far
- **$13.848 / $20.00 cap** (live, per `runs/budget_log.jsonl`) — $0.374 source audit + $2.595 chunking experiment (incl. 2 discarded truncated calls) + original $2.291 full extraction + $0.541 original Stage-0 evaluation + $0.082 material-fact-sets (2 calls) + $1.319 controlled-benchmark judging (24 calls) + $1.713 prompt-iteration experiment (12 + 2 verification calls) + $2.407 re-extraction with frozen prompt (22 calls) + $0.415 Stage-0 refresh, pre-pivot (12 calls) + $0.472 golden-pivot scoring, first (flawed) version (14 calls) + $0.489 golden-pivot scoring, corrected version (14 calls). ~$6.15 remaining for: end-to-end demo, CLI, writeup, adversarial review pass.
